
<div>
    <div>
    Carrito 
    <div id="contenido">
        <table border="1">
            <theader>
                <tr>
                    <th>Nombre producto</th>
                    <th>precio</th>
                    <th>cantidad</th>
                </tr>
            </theader>
            <tbody id="lista">
                {{#each cart.products}}
                    <tr>
                        <td>{{this.product.title}}</td>
                        <td>{{this.product.price}}</td>
                        <td>{{this.quantity}}</td>
                    </tr>
                {{/each}}
            </tbody>
        </table>
            

    </div>
    
</div>
</div>
<a href="{{host}}/products">volver</a>
<br>
<br>
 <div>
    <button id="comprar" onclick="comprar('{{host}}','{{cart._id}}')">Comprar</button> 
</div>

<script src="/js/cart.js"></script>